<h1>Übersicht Positionen</h1>

<table>
  <tr>
    <th><%= Location.human_attribute_name(:latitude) %></th>
    <th><%= Location.human_attribute_name(:logitude) %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @locations.each do |location| %>
  <tr>
    <td><%= location.latitude %></td>
    <td><%= location.longitude %></td>
    <td><%= link_to t(:show), location %></td>
    
    <% if write_access? location %>
      <td><%= link_to t(:edit), edit_location_path(location) %></td>
      <td><%= link_to t(:destroy), location, :confirm => t(:confirm_delete), :method => :delete %></td>
    <% end %>
  </tr>
<% end %>
</table>

<br />

<% if write_access? %>
<%= link_to 'Neue Position', new_location_path %>
<%  end %>