<h1><%= t(:detail)%> Auftrag</h1>

<div id="<%= dom_id(@order) %>">
  <p>
    <b><%= Order.human_attribute_name(:customer_id) %>:</b>
    <%= @order.customer %>
  </p>

  <p>
    <b><%= Order.human_attribute_name(:description) %>:</b>
    <%= @order.description %>
  </p>
  
  <p>
    <b><%= Order.human_attribute_name(:created_at) %>:</b>
    <%= l @order.created_at %>
  </p>

  <p>
    <b><%= Order.human_attribute_name(:address) %>:</b>
    <% unless @order.address.nil? %>
      <%= render :partial => "addresses/address", :locals => { :address => @order.address } %>
      <%= render :partial => "addresses/map", :locals => { :gps_position => @order.address.gps_position } %>
    <% end %>
  </p>

  <p>
    <b><%= Order.human_attribute_name(:time_entries) %>:</b>
    <%= render :partial => "accomplished_time_entries", :locals => { :time_entries => @order.time_entries } %>

  </p>

  <p>
    <b><%= TimeEntryType.human_attribute_name(:materials) %>:</b>
    <%= render 'applied_materials', :applied_materials => @order.applied_materials %>
    <%= link_to "Material hinzufügen", add_material_order_path(@order), :remote => true %>
  </p>
</div>

<%= link_to t(:edit), edit_order_path(@order) %> |
<%= link_to t(:back), orders_path %>
